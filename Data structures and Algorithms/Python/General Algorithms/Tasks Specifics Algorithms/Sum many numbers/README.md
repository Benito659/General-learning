### Sum Numbers Algoritm

The main objective is to create a function that accepts any number of numbers and returns their sum.

```python
sum_numbers(1,2,3,4)
# output: 10
```

- Requirements:
    - Use *args
    - Do not use sum()